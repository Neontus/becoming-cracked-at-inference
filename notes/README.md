# Session notes

Run `python3 scripts/new_session.py "topic"` to create a dated note under
`notes/sessions/YYYY-MM/`. These notes are intentionally rough. Keep secrets,
private datasets, credentials, and large profiler artifacts out of Git.

